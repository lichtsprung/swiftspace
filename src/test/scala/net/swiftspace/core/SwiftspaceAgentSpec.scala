package net.swiftspace.core

import org.scalatest.FunSpec

/**
 * Specification test for the SwiftspaceAgent class.
 */
class SwiftspaceAgentSpec extends FunSpec {

  describe("A SwiftspaceAgent") {
    it("should reply with a name if asked.")(pending)
    it("should reply with a list of active effects if asked.")(pending)
    it("should reply with a list of attributes if asked.")(pending)
    it("should accept new effects and execute the effect function.")(pending)
  }
}
